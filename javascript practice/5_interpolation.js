const username = 'kenn';
console.log(` ${username} is the true JS Wizard`);

const person = {
    username: 'Kenn',
    profession: 'Secret Agent'
};

console.log(`${person.username}, ${person.profession},extrodinaire`)